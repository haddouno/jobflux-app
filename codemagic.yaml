workflows:
  android-release:
    name: JOB FLUX - Android APK
    max_build_duration: 30
    environment:
      flutter: stable
    scripts:
      - name: Get Flutter packages
        script: |
          cd jobflux
          flutter pub get
      - name: Build APK
        script: |
          cd jobflux
          flutter build apk --release
    artifacts:
      - jobflux/build/app/outputs/flutter-apk/app-release.apk
    publishing:
      email:
        recipients:
          - your@email.com
        notify:
          success: true
          failure: true
